[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Laravel exposes a built-in health endpoint at /up (configured in bootstrap/app.php).
healthcheckPath = "/up"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
